{
  "name": "daco-mcp",
  "version": "1.0.0",
  "description": "DACO — Declarative Agent & MCP Orchestration. Cloudflare Worker MCP server.",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
